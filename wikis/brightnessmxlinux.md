- `sudo apt install brightnessctl`

- Download or Copy the [b](assets/b) and [b-](assets/b-) files

- `chmod +x b`

- `chmod +x b-`

- `sudo cp -r b /usr/local/bin/`

- `sudo cp -r b- /usr/local/bin/`

- Use the `b` or `b-` to increase or reduce the screen brightness
